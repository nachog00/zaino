name: CI - Nightly

on:
  schedule:
    - cron: '30 3 * * *'
  workflow_dispatch:

jobs:
  tier2-stub:
    name: Tier 2 (stub)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Tier 2 passes (stub for flow testing)
        run: echo "Tier 2 stub - all tests pass"
