[tasks.changeset-validate]
description = "Validate all .changeset/*.md files"
command = "cargo"
args = ["run", "--package", "xtask", "--", "versioning", "changeset", "validate"]

[tasks.changeset-new]
description = "Create a new changeset file (delegates to knope)"
command = "knope"
args = ["document-change"]
