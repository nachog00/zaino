[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
chrono = "0.4"
clap = { version = "4", features = ["derive"] }
console = "0.15"
semver = "1"
toml = "0.8"
serde = { version = "1", features = ["derive"] }
